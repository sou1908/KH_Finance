# Copy to .env and set the address of your API on Hostinger.
#
# Leave it blank or delete the file to run in local-only mode (everything stays
# in the browser, no login) — useful for trying the app before the server is up.
#
# No trailing slash.
VITE_API_URL=https://yourdomain.com/api
